<div class="page-header">
	<h1>Editing exam</h1>
</div>

<%= render 'form' %>

<div class="btn-group">
	<%= link_to 'Show', @exam, class: "btn btn-primary" %>
	<%= link_to 'Back to Class', @exam.classroom, class: "btn btn-default" if @exam.classroom%>
</div>
