<div class="page-header">
	<h1>Editing assignment</h1>
</div>

<%= render 'form' %>

<div class="btn-group">
	<%= link_to 'Show', @assignment, class: "btn btn-primary" %>
	<%= link_to 'Back to Class', @assignment.classroom, class: "btn btn-default" if @assignment.classroom%>
</div>
