describe "ruby" do
	describe "math" do
		it "adds 3 and 3" do
			(3 + 3).should eq(6)
		end

		it "subtracts 3 and 3" do
			(3 - 3).should eq(0)
		end

		it "multiplies 3 and 3" do
			(3 * 3).should eq(9)
		end

		it "divides 3 and 3" do
			(3 / 3).should eq(1)
		end
	end
end
